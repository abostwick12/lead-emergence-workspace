# Rollback

Rollback when reconciliation fails, access is unauthorized, login/storage
fails materially, ministry access breaks, secrets are exposed, or error rates
exceed the approved threshold. Disable Workspace writes, restore old route and
writes, preserve all Workspace evidence, revoke compromised credentials, and
document corrective work. Do not delete failed migration evidence.
