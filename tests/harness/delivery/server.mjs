import {createServer} from "vite";import path from "node:path";import {fileURLToPath} from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../../..");const harness=path.join(root,"tests/harness/delivery");
export const server=await createServer({configFile:false,root:harness,server:{host:"127.0.0.1",port:3131,strictPort:true,fs:{allow:[root]}},resolve:{alias:[{find:"./use-executive",replacement:path.join(harness,"mock-use-executive.ts")},{find:"next/link",replacement:path.join(harness,"mock-link.tsx")},{find:"next/navigation",replacement:path.join(harness,"mock-navigation.ts")},{find:"@",replacement:root}]},esbuild:{jsx:"automatic"}});await server.listen();console.log("Isolated Executive delivery component: http://127.0.0.1:3131 — no account, API, provider or database.");
